require 'spec_helper'

describe Prawn::Graph do
  it 'has a version number' do
    expect(Prawn::Graph::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
